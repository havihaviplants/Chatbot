services:
  - type: web
    name: smartdm-backend
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port 10000"
    pythonVersion: 3.10.4   # 또는 3.11.8 등 Render에서 지원하는 버전
